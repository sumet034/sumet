import React from 'react';
import { useParams } from 'react-router-dom';
const Show = () => {
    let { topicId } = useParams();

    return (
        <div>
            
        </div>
    );
}

export default Show;
